[project]
name = "bank-agent"
version = "0.1.0"
description = "A secure AI banking agent built with LangGraph"
requires-python = ">=3.12,<3.13"
dependencies = [
    "httpx[socks]>=0.28,<1",
    "langgraph>=1.0,<2",
    "langgraph-checkpoint-sqlite>=3.0,<4",
    "prompt-toolkit>=3.0,<4",
    "python-dotenv>=1.1,<2",
]

[tool.uv]
package = false
